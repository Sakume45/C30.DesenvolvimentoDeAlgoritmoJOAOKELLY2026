dados = {
    "usuário": "admin"
    "senha": "1234"
}

def verificar_acesso(usuario, senha, tentativas):
    if tentativas >= 3
    return "Bloqueado"
    elif usuario == True and senha == False:
        return "Senha incorreta."
        else:
            return "Usuário incorreta."
    else:
        return "Bem vindo, admin."